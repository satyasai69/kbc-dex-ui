/** @type {import('next').NextConfig} */
/* const nextConfig = {};

export default nextConfig;

// next.config.js

module.exports = {
  images: {
    domains: ["images.unsplash.com"],
  },
};
 */
// next.config.mjs

const nextConfig = {
  images: {
    domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
